# היסטוריית גרסאות

## v1.0 — הגרסה הראשונה המלאה

תוכנת שיחה קולית בין שני מחשבי Windows על אותה רשת מקומית. השמע עובר ישירות
בין המחשבים ב-UDP — בלי שרת, בלי אינטרנט, בלי הרשמה.

### מה נכנס

**שיחות**
* גילוי אוטומטי של מחשבים ברשת (UDP broadcast), עם הודעת פרידה ביציאה כך
  שמחשב שנסגר נעלם מהרשימה מיד ולא אחרי timeout.
* הקמת שיחה על חיבור TCP שנשאר פתוח לכל אורכה: `invite` / `ringing` /
  `accept` / `reject` / `busy` / `bye` / `ping`. נפילת החיבור = ניתוק מיידי.
* מדידת זמן הלוך-חזור, פסק זמן לשיחה שלא נענתה, ותשובת "תפוס" למתקשר שני.
* חוצץ השהיה שמסדר חבילות שהגיעו לא בסדר, מתעלם מכפולות, ומחפה על חבילה
  אבודה בדעיכה של הקודמת. כשהחוצץ מתמלא (הפרש שעונים בין המחשבים) הוא
  זורק מקטע ישן כדי שההשהיה לא תלך ותגדל.
* המרת קצב דגימה בתוכנה, כך שכל אוזנייה עובדת בכל איכות רשת.
* אורך המקטע מוגבל לפי קצב הדגימה כך שחבילה תמיד נכנסת למסגרת רשת אחת
  ולעולם לא נדרש פיצול IP.

**אוזניות USB שמתחברות אחרי הפעלת התוכנה** — הדרישה המרכזית
* זיהוי אוטומטי: חיבור אוזניות נתפס תוך שנייה-שתיים בלי ללחוץ על כלום,
  **גם באמצע שיחה**.
* הבדיקה נעשית קודם מול Windows (`waveInGetNumDevs`/`waveOutGetNumDevs`),
  שזה זול ומשקף את המצב הנוכחי, ורק כשהמספר באמת השתנה מתבצעת סריקה מלאה
  של PortAudio — שדורשת אתחול שלו ולכן עוצרת את השמע לרגע.
* סדר עדיפויות ברור: אוזניות שהתחברו זה עתה, ההתקן שכבר בשימוש, מה שנשמר
  מהפעם הקודמת, אוזניות כלשהן, ברירת המחדל של המערכת. התקן חדש שאינו
  אוזניות (מסך HDMI, למשל) לא גונב את הבחירה.
* שליפת אוזניות באמצע שיחה מזוהה, נכתבת ביומן, והתוכנה עוברת להתקן הבא.
* בחירת התקן נשמרת לפי שם ולא לפי אינדקס — אינדקסים זזים בכל חיבור מחדש.

**כתובות שמורות**
* כל כתובת שהתקשרתם אליה נשמרת, וגם כל כתובת שממנה התקשרו אליכם — כך
  שהחזרת שיחה היא לחיצה אחת.
* נשמרים גם השם וגם הפורט, כך שגם מחשב שעלה על פורט חלופי נשמר נכון.
* 12 הכתובות האחרונות, נטענות בהפעלה הבאה.

**ממשק**
* עיצוב כהה: רקע כמעט שחור, כתום כצבע הדגשה, אדום לכל מה שמסיים שיחה,
  מסגרות של פיקסל וגופן monospace. כל הצבעים ב-`lanphone/theme.py`.
* אנגלית בלבד — ל-Tk אין תמיכה בכתיבה מימין לשמאל.
* שורת כותרת כהה ובלי אייקון ב-Windows, דרך Win32 ישירות.
* אייקון: `app_icon.ico` אם קיים, אחרת ריבוע שחור פשוט שמצויר בקוד.
* סימן מים `oT` בפינה, ויומן צבוע לפי סוג ההודעה.

**אבחון תקלות**
* כתובת לא ניתנת להשגה: התוכנה משווה את הכתובת שלכם לזו שחייגתם ואומרת
  במפורש "שני המחשבים על רשתות שונות", במקום להעביר את הודעת Windows.
* מזוהים גם: חיוג לכתובת של עצמך, אין רשת כלל, וכתובת `169.254.x.x`
  (הראוטר לא חילק כתובת).
* אין מיקרופון / אין יציאת שמע — הודעה ברורה ביומן.

**אריזה ל-EXE**
* `build.bat` מייצר `dist\LANPhone.exe` — קובץ אחד שרץ בלי Python מותקן.
* `--collect-all sounddevice` — בלעדיו התוכנה עולה אבל לא מוצאת אף התקן שמע.
* קובץ אייקון פגום (PNG ששינו לו סיומת) מזוהה מראש, מדווח, ולא מפיל את
  הבנייה — התנהגות ברירת המחדל של PyInstaller היא לעצור עם `ValueError`.
* כותב ICO אמיתי מובנה (6 גדלים, בלי שום ספרייה חיצונית) כשאין קובץ אייקון.

### בדיקות

158 בדיקות אוטומטיות: המרת קצב דגימה, חוצץ ההשהיה, פורמט החבילות, מנוע
ההקלטה וההשמעה מול כרטיס קול מדומה, זיהוי אוזניות בחיבור חם, הממשק הגרפי,
הכתובות השמורות, כתיבת קובץ ICO, ושיחות מלאות מקצה לקצה (התקשרות, מענה,
דחייה, "תפוס", ניתוק, פסק זמן) על ממשק ה-loopback — בלי צורך בכרטיס קול.

### מה לא נכנס

* **אין הצפנה.** השמע עובר כ-PCM גלוי בתוך הרשת המקומית.
* **אין מבטל הד** — צריך אוזניות, לא רמקולים.
* **שיחה אחת בכל רגע.**
* **שני המחשבים חייבים להיות באותה רשת.** ראוטרים שונים דורשים הפניית
  פורטים, וזה לא נבדק.

### נבדק על

הפיתוח והבדיקות רצו על Linux. הקוד שספציפי ל-Windows — שורת הכותרת הכהה
(`winchrome.py`) וזיהוי ההתקנים דרך `winmm` — נבדק מול מוקים ולא על מכונת
Windows אמיתית.
